apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins-ingress
  namespace: rt
spec:
  ingressClassName: nginx
  rules:
  - host: jenkins.IP_LB_PLACEHOLDER.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jenkins-svc
            port:
              number: 80