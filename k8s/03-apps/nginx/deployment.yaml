apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-forge
  namespace: rt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-forge
  template:
    metadata:
      labels:
        app: web-forge
    spec:
      containers:
        - image: thfx31/ynov:forge-v1
          name: nginx
